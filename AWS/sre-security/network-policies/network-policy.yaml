# sre-security/network-policies/network-policy.yaml

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: poseidon-demo
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
